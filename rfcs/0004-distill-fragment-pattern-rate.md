---
state: blocked
state_since: 2026-08-16
---

## タスク

**断片パターンの率は ADR-0084 後置ゲートで大きく下がった — 台帳の着手条件（「率が下がっていなければ着手」）により着手しない**。2026-08-01 実測（`knowledge.json` 4,874 行、`distilled` 日付で pre/post 分割、末尾の閉じ引用符・アスタリスク・括弧を剥がしてから終止符を判定）: 途中切れ **3.52% → 1.02%**、括弧不均衡 **1.89% → 0.34%**（post = 07-26 以降 586 行）。後置ゲートは durability を判定するついでに断片も落としている。**留保**: 台帳の旧値 2.9% を出した検出器と同一とは限らない（末尾引用符を剥がさないと同じデータが 5.7% に見える）— ただし pre/post は同一検出器での比較なので低下の方向は確か。残る 1.02% は `_is_valid_pattern` が長さ下限とメタ文言しか見ず構造的完結性を見ていないことに由来し、**構造的性質なので code で判定できる**（`when-code-when-llm`）が、率がこの水準なら投資に値しない

## 着手条件

再開条件: 断片パターン率が 3% 台へ回帰（現在 1.02%、ADR-0084 後置ゲート後の実測）
照合先:   `knowledge.json` の縦断再測定（手順は詳細リンク先の実測メモと同一 — 末尾の閉じ引用符・アスタリスク・括弧を剥がしてから終止符を判定）
成立時:   ready（構造的完結性の検査は code で判定可能 — `when-code-when-llm`）

## 詳細

[ADR-0084 Consequences](../docs/adr/0084-post-distill-durability-gate.md)、`distill._is_valid_pattern`

## 2026-08-17 S1 の読み（`.notes/triage-readings-2026-08-17.md`）と判定

再測（同一手順、検出器の同一性は pre バケットで 08-01 の値 3.50% / 1.91% を再現して確認）: post（07-26 以降、n=2,006）途中切れ **1.25%** / 括弧不均衡 **0.75%**。前回 1.02% / 0.34%（n=586）から微増だが 3% 台への回帰なし → **`blocked` 維持**。次の照合も同じ手順（`.notes/distill-fragment-recount-20260817.py`）。

## 2026-08-19 triage 照合（無人 cycle）

dead-band（08-17 の再測から状態・条件文とも不変）。未再読。

## 2026-08-22 triage 照合（無人 cycle）

dead-band（08-17 の再測から状態・条件文とも不変）。未再読。

## 2026-08-24 triage 照合（手動 cycle）

dead-band（08-17 の再測から状態・条件文とも不変）。未再読。

旧 ID: T-DISTILL-FRAGMENT（.notes/tasks から 2026-08-25 移送）。
本文中の `.notes/…` はローカルの作業ノート（gitignored、clone 先には存在しない）を指す。

## Status

blocked（≈ issue-tracker 標準の blocked。RFC 標準に state 語彙は無い） — 断片パターン率は ADR-0084 後置ゲートで低下し（2026-08-17 再測: 途中切れ
1.25% / 括弧不均衡 0.75%）、3% 台への回帰待ち（2026-08-25）。直近の照合 2026-08-24 は
dead-band（状態・条件文とも不変で未再読）。

## Next action

- 再開条件: 断片パターン率が 3% 台へ回帰
- 照合先: `knowledge.json` の縦断再測定（手順は `.notes/distill-fragment-recount-20260817.py`
  と同一 — 末尾の閉じ引用符・アスタリスク・括弧を剥がしてから終止符を判定）
- 成立時: ready（構造的完結性の検査は code で判定可能 — `when-code-when-llm`）
