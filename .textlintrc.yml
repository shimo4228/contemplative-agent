# textlint — .claude/verify.sh の prose category（staged のみ）。
# 選定の経緯と実測値は .claude/verify.md。
#
# 方針: **目で見えない文字化けだけ**を止める。人間のレビューで気づける層
# （一文が長い、読点が多い、二重否定）は入れない — この repo には既に文体の
# 正本があり（ハーネス側 writing-ecosystem の Voice 規約、docs/glossary.md の
# 訳語規約）、preset の編集方針を並べると「第 2 の権威」になって、毎回どちらが
# 正しいかを判断する羽目になる。
#
# 導入時点の違反は 0 件（日本語を含む tracked .md 107 本で実測、2026-07-31）。
# これは drain すべき負債が無いという意味であり、**ゲートを入れるのに最も安い
# タイミング**でもある。ここで得るのは過去の修正ではなく将来の退行防止。
rules:
  preset-ja-technical-writing:
    # --- 有効: 混入すると読めるのに気づけない層 ---
    # 出所は web からの copy-paste、macOS のファイル名（NFD）、レガシー資料。
    # 実測 0 件だが、違反を注入して 3 ルールの発火を確認済み。
    no-hankaku-kana: true              # ﾊﾝｶｸ ｶﾅ
    no-zero-width-spaces: true         # U+200B
    no-nfd: true                       # 濁点が合成文字（が = か + ゛）
    no-invalid-control-character: true # 制御文字
    no-dropping-the-ra: true           # ら抜き言葉

    # --- 無効: 文体の判断であって、既存の Voice 規約と競合する ---
    sentence-length: false             # 既定 100 字だと日本語 6156 文中 621 文が該当
    max-comma: false
    max-ten: false
    max-kanji-continuous-len: false
    arabic-kanji-numbers: false
    no-mix-dearu-desumasu: false       # だ/である と ですます の使い分けは規約側が持つ
    ja-no-mixed-period: false
    no-doubled-joshi: false
    no-doubled-conjunction: false
    no-doubled-conjunctive-particle-ga: false
    no-double-negative-ja: false       # 「〜ないわけではない」は精密な表現として使う
    no-exclamation-question-mark: false
    ja-no-weak-phrase: false
    ja-no-redundant-expression: false
    ja-no-abusage: false

    # --- 無効: この repo の記法に対して偽陽性しか出さない（実測）---
    # 日本語 prose を手で折り返すので、（ が行末で ） が次行に来る。25 件全てこれ。
    no-unmatched-pair: false
    # 「経路 a、b、c」の列挙ラベルを「不自然なアルファベット」として上げる。
    ja-unnatural-alphabet: false
    # glossary.md の中国語訳カラム（反事实实验）を「同語の連続」として上げる。
    ja-no-successive-word: false
