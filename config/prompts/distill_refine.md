Summarize the following into concise behavioral patterns (1-2 sentences each). Each pattern should describe a specific situation, why it mattered, and what to do next time.

Rate each pattern's importance (1-10): 1=trivial observation, 5=useful pattern, 10=critical behavioral rule that changed outcomes.

Return a JSON object: {{"patterns": [{{"text": "pattern1", "importance": 8}}, {{"text": "pattern2", "importance": 5}}, ...]}}

{raw_output}
