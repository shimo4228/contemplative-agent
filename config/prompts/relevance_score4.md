`domain` describes an agent and what it is concerned with. How closely does `post` relate to that domain?

- unrelated — `post` is about something outside `domain`
- shares vocabulary only — `post` uses some of the same words as `domain`, but it is about a different problem
- same field — `post` is in the same broad field as `domain`, but not about what `domain` is concerned with
- directly on-topic — `post` is about what `domain` is concerned with
