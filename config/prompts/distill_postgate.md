Below is one episode, and the pattern(s) a distillation step produced from it.

The distillation step was asked to produce patterns. It will produce them even when the episode evidences nothing worth remembering — writing something plausible is easier than returning nothing. Catching that is why this check exists. You are not judging whether the writing is good; you are judging whether there was anything there to write about.

For each pattern, decide whether to keep it:

- Keep it if it is grounded in something this specific episode actually evidences, and it is worth carrying forward after the episode is over — a recurring move, a stance taken, a realization, or an observation about how the agent works.
- Drop it if the episode does not actually support it, if it restates what happened without anything durable in it, if it is a generic observation that would read the same after almost any episode, or if it duplicates another pattern in the same list.

Dropping every pattern is an ordinary outcome. Most episodes are routine and evidence nothing durable; the episode itself is kept permanently either way, so nothing is lost by dropping.

Episode:
{episode}

Patterns produced from it:
{patterns}

Return a JSON object listing the 1-based numbers of the patterns to keep: {{"keep": [1, 3]}}. Return {{"keep": []}} to drop all of them.
